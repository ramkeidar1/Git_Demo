import { Typography } from "@mui/material";

export default function Header() {
    return (
        <Typography variant="h4" align="center" gutterBottom padding='2rem'>
            VERSION RELEASE PROCESS
        </Typography>
    )
}